name: Create and update github repo
on:
  workflow_dispatch:
  push:

jobs:
 gitreposettings:
    runs-on: ubuntu-latest        
    env:
      GH_TOKEN: ${{ secrets.REPO_SECRET }}    
      OWNER: ${{ github.repository_owner }}
      REPO: ${{ github.event.repository.name }}
      
    steps:    
        - name: Github Action for curl
          uses: wei/curl@v1.1.1

        - name: update github repo
          run: |
           gh api \
           --method PATCH \
           -H "Accept: application/vnd.github+json" \
           /repos/prasha94/test \
           -f name='test' \
           -f description='changes are made to this repo settings' \
           -f homepage='https://github.com/prasanthksalla/REPO' \
           -F private=true \
           -F has_wiki=true 
        
        - name: adding teams to organization project
          run: |
           gh api \
           --method PUT \
           -H "Accept: application/vnd.github+json" \
           /orgs/prasanthksalla/teams/whr_data_leads/repos/prasanthksalla/test \
           -f permission='maintain'             

           gh api \
           --method PUT \
           -H "Accept: application/vnd.github+json" \
           /orgs/prasanthksalla/teams/whr_devopsautomation/repos/prasanthksalla/test \
           -f permission='admin' 
           
           gh api \
           --method PUT \
           -H "Accept: application/vnd.github+json" \
           /orgs/prasanthksalla/teams/whr_data_devs/repos/prasanthksalla/test \
           -f permission='maintain' 
           
        - name: adding branch protection rules
          run: |
            curl \
            -X PUT \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.REPO_SECRET }}" \
            https://api.github.com/repos/prasanthksalla/test/branches/main/protection \
            -d '{"required_status_checks":{"strict":true,"contexts":["Reports"]},"enforce_admins":null,"required_pull_request_reviews":{"required_approving_review_count":1},"restrictions":null}'

            curl \
            -X PUT \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.REPO_SECRET }}" \
            https://api.github.com/repos/prasanthksalla/test/branches/dev/protection \
            -d '{"required_status_checks":{"strict":true,"contexts":["Reports"]},"enforce_admins":null,"required_pull_request_reviews":{"required_approving_review_count":1},"restrictions":null}'

        - name: adding webhook
          run: |
            curl \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.REPO_SECRET }}" \
            https://api.github.com/repos/prasanthksalla/test/hooks \
            -d '{"name":"web","active":true,"events":["push"],"config":{"url":"https://example.com/git-webhook","content_type":"form","insecure_ssl":"0"}}'
